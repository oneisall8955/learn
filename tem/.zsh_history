curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -9DC8 5822 9FC7 DD38 854A E2D8 8D81 803C 0EBF CD88
cat /etc/os-release
apt-key fingerprint 0EBFCD88
lsb_release -cs
add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/debian $(lsb_release -cs) stable"
apt-get update
apt-get install docker-ce docker-ce-cli containerd.io
docker run hello-world
systemctl status docker.service

curl -L "https://github.com/docker/compose/releases/download/1.24.1/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose
ln -s /usr/local/bin/docker-compose /usr/bin/docker-compose
docker-compose --version

cd /opt/software


mkdir bitwarden && cd bitwarden


vim config.env

vim docker-compose.yml

cat config.env

j ng
cd /opt/software/nginx/conf.d


vim php_test.conf
cp php_test.conf bw.tem.conf
vim bw.tem.conf

vim bw.tem.conf
rm bw.tem.conf

 /opt/temp/ssl
j ty

./https_support.sh bitwarden.liuzhicong.cn
cp /opt/temp/ssl/bitwarden.liuzhicong.cn.zip /opt/software/nginx/ssl
./https_support.sh bitwarden.liuzhicong.cn

nginx -t
-


vim bitwarden.liuzhicong.cn_https_ssl.conf

nginx -t
vim bitwarden.liuzhicong.cn_https_ssl.conf
-
./https_support.sh bitwarden.liuzhicong.cn
-
vim bitwarden.liuzhicong.cn_https_ssl.conf
nginx -t
docker-compose up -d
systemctl restart nginx.service
docker --list
docker ps

htop

exit

cd

cd ~/.oh-my-zsh
git status
git add -A
git commit -m "my custom"

exit

cd

cd /opt/temp/

 -a

cd /opt/software/bitwarden

vim config.env

docker-compose down && docker-compose up -d
ifconfig


 data
sqlite3 data/bitwarden.db

htop

export LANG="en_US.UTF-8";export LANGUAGE="en_US.UTF-8";top

vim config.env

docker-compose down && docker-compose up -d
ifconfig

docker-compose down
ifconfig

docker-compose up -d

ifconfig
exit
 data
sqlite3 data/bitwarden.db

htop


exit

docker logs bitwarden-nginx
docker logs bitwarden

vim config.env
vim ~/.zsh_history
vim config.env


vim docker-compose.yml

cat docker-compose.yml
docker ps
docker -h
docker --help
docker images
docker pull bitwardenrs / server＃�latest
docker logs bitwarden
